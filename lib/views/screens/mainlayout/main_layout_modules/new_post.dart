import 'package:flutter/widgets.dart';

class NewPostModule extends StatelessWidget {
  const NewPostModule({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('NewPost');
  }
}