import 'package:flutter/widgets.dart';

class ChatsModule extends StatelessWidget {
  const ChatsModule({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('chats');
  }
}